// const _ = require('lodash');

// const object = {

// };

// const other: { a: string[{ c: number, e: number }] } = {
//     'a': [{ 'c': 4 }, { 'e': 5 }],
// };
// const other3 = {
//     'a': [{ 'd': 4 }, { 'x': 5 }],
// };

// console.log(_.merge(object, other, other3));

// // => { 'a': [{ 'b': 2, 'c': 3 }, { 'd': 4, 'e': 5 }] }
